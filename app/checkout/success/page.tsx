import Link from "next/link";import { CheckCircle2 } from "lucide-react";import { ClearCartOnMount } from "./ClearCartOnMount";export const metadata = { title: "Order Confirmed" };export default async function SuccessPage({  searchParams,}: {  searchParams: Promise<{ session_id?: string }>;}) {  const { session_id } = await searchParams;  return (    <section className="mx-auto max-w-2xl px-4 py-20 text-center sm:px-6 lg:px-8">      <ClearCartOnMount />      <CheckCircle2 className="mx-auto h-14 w-14 text-emerald-600" />      <h1 className="mt-6 text-3xl font-bold tracking-tight text-navy-950">        Thank you for your order.      </h1>      <p className="mt-3 text-base text-slate-600">        We&rsquo;ve received your order and emailed you a receipt. A team member will        confirm shipping details within one business day.      </p>      {session_id && (        <p className="mt-2 text-xs text-slate-400">          Reference: <span className="font-mono">{session_id}</span>        </p>      )}      <div className="mt-8 flex justify-center gap-3">        <Link          href="/shop"          className="inline-flex rounded bg-navy-900 px-5 py-2.5 text-sm font-semibold text-white hover:bg-navy-800"        >          Continue Shopping        </Link>        <Link          href="/"          className="inline-flex rounded border border-slate-300 px-5 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-100"        >          Return Home        </Link>      </div>    </section>  );}